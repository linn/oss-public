// Define Kinsky Pronto Build Version
var buildVersion = "4.1.1";
var buildRevision, buildTimestamp;

if (!buildRevision) {
  buildRevision = "$Revision$";
};

if (!buildTimestamp) {
  buildTimestamp = "mm:mm:ss";
};
