////
//
// uiPageXofY.js
//
// Currently unused, this package will be used to display a page x of y
// pop-over in the bottom RHS of ony screens that need it.
//
// If there is only one page then nothing will be displayed.
//
////
var uiPageXofY = {};

uiPageXofY.display = function(entry, entries, pageSize){
/*
  if (entry < 0) {
    return "";
  };

  // return "page 1 of 3"
  //
  var div = parseInt(entry / pageSize);
  var noPages = (parseInt((totalEntries-1)/pageSize)) + 1; // 0 offset & all that.

  return "[ " + (div+1) + " : " + noPages + " ]";
*/
};

elab.add("uiPageXofY");
